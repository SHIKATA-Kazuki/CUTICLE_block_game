// ============================================================
// ゲーム全体の設定値
// 数値を変えるだけで難易度やサイズを調整できます。
// ============================================================
const CONFIG = {
  // 画面サイズ（スマホの縦持ちを想定した比率）
  CANVAS_WIDTH: 360,
  CANVAS_HEIGHT: 640,

  // パドル（バー）
  PADDLE_WIDTH: 74,
  PADDLE_HEIGHT: 12,
  PADDLE_BOTTOM_MARGIN: 30, // 画面下端からの距離
  PADDLE_SPEED: 6,          // 1フレームあたりの移動量（大きいほど速い）

  // ボール
  BALL_RADIUS: 6,
  BALL_SPEED: 4.6,

  // ライフ
  INITIAL_LIVES: 3,

  // ブロックエリアのレイアウト
  // ※ ドット絵レベル（LEVEL）は列数が多いので、余白と隙間を小さめにしてある。
  //    ブロックは正方形になるよう、幅から自動で高さも決まる（blocks.js参照）。
  BLOCK_AREA_TOP: 40,          // 一番上のブロックまでの距離
  BLOCK_AREA_SIDE_PADDING: 6,  // 画面左右の余白
  BLOCK_GAP: 0,                // ブロック同士の隙間（0=隙間なしでドット絵っぽくする）

  // アイテム（お助けブロックから落ちてくるもの）
  ITEM_SIZE: 20,          // アイテムの大きさ（正方形の一辺）
  ITEM_FALL_SPEED: 2.6,   // アイテムが落ちる速さ

  // アイテムを持たせるブロックの数（毎回ランダムに選び直す）
  ITEM_BLOCK_RATIO: 0.015, // 総ブロック数に対する割合の目安
  ITEM_BLOCK_MIN: 3,       // 最低でもこの個数は用意する
  ITEM_BLOCK_MAX: 25,      // 多くなりすぎないための上限

  // マルチボール
  MAX_BALLS: 9,           // 同時に存在できるボールの最大数（増えすぎ防止）
};
